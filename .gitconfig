[alias]
	dotfiles = !git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME
[user]
	email = mike@mwm-webdesign.com
	name = Michael Wassmer
[init]
	defaultBranch = main
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
